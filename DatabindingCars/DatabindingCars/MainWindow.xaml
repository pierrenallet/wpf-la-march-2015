<Window x:Class="DatabindingCars.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DatabindingCars"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
            
        <ListBox Grid.Column="0" Grid.Row="1" ItemsSource="{Binding}" IsSynchronizedWithCurrentItem="True"/>
        <ListBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding /Models}" IsSynchronizedWithCurrentItem="True"/>
        
        <!--<TreeView ItemsSource="{Binding}"/>-->
        
        <Menu>
            <MenuItem Header="Brands" ItemsSource="{Binding}"/>
        </Menu>
    </Grid>
</Window>
